import { Table } from '../../components/Table/Table';
import Section from "../../components/Section/Section";

export const Admin = () => {
    return (
        <Section>
           <Table />
        </Section>
    );
}